/*
 * Write a function ```int convert()``` that reads a decimal number one character 
 * (Digit) at a time, terminated by a blank, and returns the value of that number.
 */
int convert() {
	int x
	while (scanf("%d", &x
}
